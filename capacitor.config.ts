import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'pi-pokemons-ionic-react',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
